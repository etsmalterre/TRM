// Tableau de bord types — shared verbatim with ETM (single source of truth,
// see CLAUDE.md § Shared screens). This shim exists so the shared shell's
// `@/components/dashboard/types` imports resolve inside THIS app.
export * from '@etm/components/dashboard/types'
